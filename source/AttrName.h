#pragma once

/*
	AttrName is an enum class that provides an easy representation
	of each attribute name identified in the query parsed
*/

enum class AttrName {
	PROC_NAME,
	VAR_NAME,
	VALUE,
	STMT_NUM,
	NONE
};